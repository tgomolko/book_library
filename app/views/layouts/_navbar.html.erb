 <nav class="navbar is-dark" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <%= link_to root_path, class:"navbar-item" do %>
      <h1 class="title is-5 has-text-white">BookLibrary</h1>
    <% end  %>
    <div class="navbar-burger burger" data-target="navbar">
      <span></span>
      <span></span>
      <span></span>
    </div>
  </div>

  <div id="navbar" class="navbar-menu">
    <div class="navbar-end">
      <div class="navbar-item">

        <% if user_signed_in? %>

          <%= link_to 'New Book', new_book_path, class: 'button is-dark' if current_user.admin? %>

          <div class="field is-grouped">
            <p class="control">
              <%= link_to "Create Product", new_product_path, class:"navbar-item button is-outlined is-info"  if current_user.admin? %>
            </p>

            <p class="control">
              <%= link_to "Create Plan", new_plan_path, class:"navbar-item button is-outlined is-success"  if current_user.admin? %>
            </p>

             <p class="control">
              <%= link_to "Add Card", add_payment_method_path, class:"navbar-item button is-outlined is-info" %>
            </p>

            <%= render "layouts/profile/actions"%>

          <% else %>
            <p class="control">
              <%= link_to 'Pricing', pricing_path, class: 'navbar-item button is-dark' %>
            </p>
            <p class="control">
              <%= link_to "Sign In", new_user_session_path, class:"navbar-item button is-dark" %>
            </p>
            <p class="control">
              <%= link_to "Sign up", new_user_registration_path, class:"navbar-item button is-dark"%>
            </p>
          <% end %>
        </div>

    </div>
  </div>
</div>
</nav>
