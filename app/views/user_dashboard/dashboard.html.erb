<%= render "menu" %>
<%= link_to "Download Receipt", generate_bill_path(id: @charge.id, format: :pdf) %>

<!--
<%= simple_form_for :search, url: books_path, method: "GET" do |f| %>
  <%= f.input :flavors, label: "Category",  collection: Book.pluck(:title), as: :check_boxes %>
  <%#= f.input :strengths, label: "Flavor", collection: $flavors, as: :check_boxes  %>
  <%= f.submit "Search", class: "btn btn-primary m" %>
  <%= link_to "Reset", root_path  %>
<% end %>
 -->
